#include <stdio.h>
/**
 * main - prints its name,
 * @argc: number of arguments
 * @argv: array of pointets to the arguments
 *
 *Return: 0
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
