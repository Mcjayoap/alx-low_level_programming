#include "main.h"
/**
 * _sqrt_recursion - returns the natural square root
 *  of a number.
 *  @n: integer
 *  Return: 0
 */
int _sqrt_recursion(int n)
{
	int c;

	if (c == n)
	{
		return (0);
	}
	return (0);
}
